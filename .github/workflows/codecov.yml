# This is not usual github action workflow
# This file configures Codecov used for coverage reports.

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0%
        base: auto
#       todo replace with main branch
        branches: [feature/sdk_v3.0/root]
        if_ci_failed: error
